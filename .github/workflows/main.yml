on: push
jobs:
  example-job:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./interaction/snippets
    steps:
      - name: test
        run:  devnet-mjex-xmph.snippets.sh deploy
